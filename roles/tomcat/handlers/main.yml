---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart openspecimen
  ansible.builtin.systemd:
    name: "{{ openspecimen_service_name | default('openspecimen') }}"
    state: restarted
