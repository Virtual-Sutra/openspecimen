{# Per-app datasource + config block, MERGED into the shared conf/context.xml by
   blockinfile (openspecimen_shared_tomcat mode). Standalone <Resource>/<Environment>
   with NO <Context> wrapper - blockinfile inserts these before the shared
   </Context>, keyed to war_name so co-tenant apps in one Tomcat each bind their
   own DB while any co-tenant (incl. a legacy hand-managed app) block survives. #}
{% if db_type == 'oracle' %}
<Resource name="{{ openspecimen_datasource_name | default('jdbc/openspecimen') }}"
          auth="Container"
          type="javax.sql.DataSource"
          factory="org.apache.tomcat.jdbc.pool.DataSourceFactory"
          driverClassName="oracle.jdbc.OracleDriver"
{% if oracle_db_sid is defined and oracle_db_sid %}
          url="jdbc:oracle:thin:@{{ oracle_db_host }}:{{ oracle_db_port }}:{{ oracle_db_sid }}"
{% else %}
          url="jdbc:oracle:thin:@//{{ oracle_db_host }}:{{ oracle_db_port }}/{{ oracle_db_service }}"
{% endif %}
          username="{{ oracle_db_user }}"
          password="{{ oracle_db_password }}"
          maxActive="{{ tomcat_pool_max_active }}"
          minIdle="{{ tomcat_pool_min_idle }}"
          initialSize="{{ tomcat_pool_initial_size }}"
          maxWait="{{ tomcat_pool_max_wait_ms }}"
          testWhileIdle="true"
          testOnBorrow="true"
          validationQuery="select 1 from dual"
          validationInterval="{{ tomcat_pool_validation_interval_ms }}"
          timeBetweenEvictionRunsMillis="{{ tomcat_pool_eviction_interval_ms }}"
          minEvictableIdleTimeMillis="{{ tomcat_pool_min_evictable_idle_ms }}"
          removeAbandoned="true"
          removeAbandonedTimeout="{{ tomcat_pool_abandoned_timeout_s }}"
          logAbandoned="true"
          abandonWhenPercentageFull="{{ tomcat_pool_abandon_pct }}"
          jmxEnabled="true"
          jdbcInterceptors="org.apache.tomcat.jdbc.pool.interceptor.ConnectionState;org.apache.tomcat.jdbc.pool.interceptor.StatementFinalizer"
/>
{% else %}
<Resource name="{{ openspecimen_datasource_name | default('jdbc/openspecimen') }}"
          auth="Container"
          type="javax.sql.DataSource"
          factory="org.apache.tomcat.jdbc.pool.DataSourceFactory"
          driverClassName="com.mysql.cj.jdbc.Driver"
          url="jdbc:mysql://{{ mysql_db_host }}:3306/{{ mysql_db_name }}?useSSL=false&amp;allowPublicKeyRetrieval=true&amp;serverTimezone=UTC"
          username="{{ mysql_db_user }}"
          password="{{ mysql_db_password }}"
          maxActive="{{ tomcat_pool_max_active }}"
          minIdle="{{ tomcat_pool_min_idle }}"
          initialSize="{{ tomcat_pool_initial_size }}"
          maxWait="{{ tomcat_pool_max_wait_ms }}"
          testWhileIdle="true"
          testOnBorrow="true"
          validationQuery="select 1"
          validationInterval="{{ tomcat_pool_validation_interval_ms }}"
          timeBetweenEvictionRunsMillis="{{ tomcat_pool_eviction_interval_ms }}"
          minEvictableIdleTimeMillis="{{ tomcat_pool_min_evictable_idle_ms }}"
          removeAbandoned="true"
          removeAbandonedTimeout="{{ tomcat_pool_abandoned_timeout_s }}"
          logAbandoned="true"
          abandonWhenPercentageFull="{{ tomcat_pool_abandon_pct }}"
          jmxEnabled="true"
          jdbcInterceptors="org.apache.tomcat.jdbc.pool.interceptor.ConnectionState;org.apache.tomcat.jdbc.pool.interceptor.StatementFinalizer"
/>
{% endif %}
<Environment name="config/{{ openspecimen_war_name }}"
             value="{{ catalina_base }}/conf/{{ openspecimen_war_name }}.properties"
             type="java.lang.String"
/>
