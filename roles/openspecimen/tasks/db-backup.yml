---
# Standalone, on-demand database backup (#98) - local MySQL.
#
# Primary use: large databases where the upgrade's size gate halts (auto-dump is
# skipped to protect the deploy SLA). Run this first, then re-run the upgrade
# with -e db_backup_confirmed=true. Also usable as a general day-2 DB backup.
#
# --single-transaction takes a consistent InnoDB snapshot WITHOUT locking, so
# this can run while the application is still serving (no outage).
# --add-drop-database --databases makes the dump self-restoring (drops+recreates
# the schema on restore).
#
# RDS -> take an AWS snapshot instead (#99). Oracle -> DBA backup tooling.

- name: Assert this is a managed local MySQL database
  ansible.builtin.assert:
    that:
      - db_managed | default(true) | bool
      - db_type | default('mysql') == 'mysql'
    fail_msg: >-
      db-backup.yml supports local MySQL (db_managed=true, db_type=mysql) only.
      For RDS take an AWS snapshot (#99); for Oracle use the DBA's backup tools.

- name: Get backup timestamp
  ansible.builtin.command: date '+%d%m%Y_%H%M%S'
  register: _db_backup_ts
  changed_when: false

- name: Ensure DB backup directory exists
  ansible.builtin.file:
    path: "{{ db_backup_dir | default('/usr/local/openspecimen/db-backups') }}"
    state: directory
    owner: "{{ tomcat_user }}"
    group: "{{ tomcat_user }}"
    mode: '0750'

- name: Set DB backup file path
  ansible.builtin.set_fact:
    _db_backup_file: "{{ db_backup_dir | default('/usr/local/openspecimen/db-backups') }}/{{ _db_backup_ts.stdout }}_{{ mysql_db_name }}.sql.gz"

- name: Dump database (consistent, gzipped)
  ansible.builtin.shell: |
    set -o pipefail
    mysqldump --single-transaction --routines --triggers --add-drop-database --databases {{ mysql_db_name }} \
      -u {{ mysql_db_user }} -p'{{ mysql_db_password }}' -h {{ mysql_db_host }} \
      | gzip > "{{ _db_backup_file }}"
  args:
    executable: /bin/bash
  no_log: true

- name: Set ownership of the dump
  ansible.builtin.file:
    path: "{{ _db_backup_file }}"
    owner: "{{ tomcat_user }}"
    group: "{{ tomcat_user }}"
    mode: '0640'

- name: Stat the dump
  ansible.builtin.stat:
    path: "{{ _db_backup_file }}"
  register: _db_backup_stat

- name: Database backup complete
  ansible.builtin.debug:
    msg:
      - "Database backup written:"
      - "  {{ _db_backup_file }}  ({{ (_db_backup_stat.stat.size / 1024 / 1024) | round(1) }} MB gzipped)"
      - "Restore later with db-restore.yml (-e db_restore_confirm=true), or manually:"
      - "  gunzip -c {{ _db_backup_file }} | mysql -u {{ mysql_db_user }} -p -h {{ mysql_db_host }}"
      - "Then re-run the upgrade with -e db_backup_confirmed=true."
